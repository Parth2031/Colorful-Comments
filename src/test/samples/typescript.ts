
export class TestClass
{
  /**
   * Test Method
   * ! Colorful
   * * Important information is highlighted
   * & Deprecated method, do not use
   * ? Should this method be exposed through API?
   * ^ refactor this method to conform to API
   * @param param The parameter for this method
   */

  public TestMethod(param: any): void
  {
    let testVar: number = 123;

    // ~ This is a pointing out

    if (testVar > 0) {
        throw new TypeError();     // ! This is an alert
    }

    // ? This is a query
    let x = 1;

    //// this.lineOfCode() == commentedOut;

    // ^ write some test cases
  }

  //! no spaces
  // ~ Space then Tab
  // & Tab then Tab
  // ^ Tab then space

    // ? 汉字
    // * 123

  /*
   Test Method
   * Important information is highlighted
   ! Deprecated method, do not use
   ? Should this method be exposed through API?
   ^ refactor this method to conform to API
   @param param The parameter for this method
  */
}

export class TestClass {

    /**
     * Test Method
     * * Important information is highlighted
     * ! Deprecated method, do not use
     * ? Should this method be exposed through API?
     * ^ refactor this method to conform to API
     * @param param The parameter for this method
     */
    public TestMethod(param: any): void {
        let testVar: number = 123;

        // * This is a highlight
        if (testVar > 0) {
            throw new TypeError(); // ! this is an alert
        }

        // ? This is a query
        let x = 1;

        //// this.lineOfCode() == commentedOut;

        // ^ write some test cases
    }

    //!no spaces
    // !    Space then Tab
    //  !   Tab then Tab
    //  ! Tab then space

    // ? 汉字
    // * 123

    /*
     Test Method
     * Important information is highlighted
     ! Deprecated method, do not use
     ? Should this method be exposed through API?
     ^ refactor this method to conform to API
     @param param The parameter for this method
     */
}

export class TestClass {

    /**
     * Test Method
     * * Important information is highlighted
     * ! Deprecated method, do not use
     * ? Should this method be exposed through API?
     * ^ refactor this method to conform to API
     * @param param The parameter for this method
     */
    public TestMethod(param: any): void {
        let testVar: number = 123;

        // * This is a highlight
        if (testVar > 0) {
            throw new TypeError(); // ! this is an alert
        }

        // ? This is a query
        let x = 1;

        //// this.lineOfCode() == commentedOut;

        // ^ write some test cases
    }

    //!no spaces
    // !    Space then Tab
    //  !   Tab then Tab
    //  ! Tab then space

    // ? 汉字
    // * 123

    /*
     Test Method
     * Important information is highlighted
     ! Deprecated method, do not use
     ? Should this method be exposed through API?
     ^ refactor this method to conform to API
     @param param The parameter for this method
     */
}

export class TestClass {

    /**
     * Test Method
     * * Important information is highlighted
     * ! Deprecated method, do not use
     * ? Should this method be exposed through API?
     * ^ refactor this method to conform to API
     * @param param The parameter for this method
     */
    public TestMethod(param: any): void {
        let testVar: number = 123;

        // * This is a highlight
        if (testVar > 0) {
            throw new TypeError(); // ! this is an alert
        }

        // ? This is a query
        let x = 1;

        //// this.lineOfCode() == commentedOut;

        // ^ write some test cases
    }

    //!no spaces
    // !    Space then Tab
    //  !   Tab then Tab
    //  ! Tab then space

    // ? 汉字
    // * 123

    /*
     Test Method
     * Important information is highlighted
     ! Deprecated method, do not use
     ? Should this method be exposed through API?
     ^ refactor this method to conform to API
     @param param The parameter for this method
     */
}

export class TestClass {

    /**
     * Test Method
     * * Important information is highlighted
     * ! Deprecated method, do not use
     * ? Should this method be exposed through API?
     * ^ refactor this method to conform to API
     * @param param The parameter for this method
     */
    public TestMethod(param: any): void {
        let testVar: number = 123;

        // * This is a highlight
        if (testVar > 0) {
            throw new TypeError(); // ! this is an alert
        }

        // ? This is a query
        let x = 1;

        //// this.lineOfCode() == commentedOut;

        // ^ write some test cases
    }

    //!no spaces
    // !    Space then Tab
    //  !   Tab then Tab
    //  ! Tab then space

    // ? 汉字
    // * 123

    /*
     Test Method
     * Important information is highlighted
     ! Deprecated method, do not use
     ? Should this method be exposed through API?
     ^ refactor this method to conform to API
     @param param The parameter for this method
     */
}

export class TestClass {

    /**
     * Test Method
     * * Important information is highlighted
     * ! Deprecated method, do not use
     * ? Should this method be exposed through API?
     * ^ refactor this method to conform to API
     * @param param The parameter for this method
     */
    public TestMethod(param: any): void {
        let testVar: number = 123;

        // * This is a highlight
        if (testVar > 0) {
            throw new TypeError(); // ! this is an alert
        }

        // ? This is a query
        let x = 1;

        //// this.lineOfCode() == commentedOut;

        // ^ write some test cases
    }

    //!no spaces
    // !    Space then Tab
    //  !   Tab then Tab
    //  ! Tab then space

    // ? 汉字
    // * 123

    /*
     Test Method
     * Important information is highlighted
     ! Deprecated method, do not use
     ? Should this method be exposed through API?
     ^ refactor this method to conform to API
     @param param The parameter for this method
     */
}